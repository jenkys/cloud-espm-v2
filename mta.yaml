ID: cloudespmv2
_schema-version: '2.1'
version: 0.0.1

modules:
 - name: espmcloudjpa
   type: java
   path: espm-cloud-jpa
   parameters:
      memory: 512M
   provides:
    - name: espmcloudjpa_api
      properties:
         url: ${default-url}
